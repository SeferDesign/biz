<% headers = [
  'Paid Date',
  'Client',
  'Amount',
  'Payment Type',
  'Description'
] %>
<%= CSV.generate_line headers %>
<% @year.invoices.paid.sort { |a,b| a.paiddate <=> b.paiddate }.each do |invoice| %><%= CSV.generate_line([
    invoice.paiddate,
    invoice.client.name,
    invoice.cost,
    invoice.paymenttype,
    invoice.description
  ]) %><% end %>
