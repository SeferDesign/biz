<header class="pad">
	<div class="container">
		<div class="row">
			<div class="col-sm-4 col-sm-offset-4 text-center">
				<img src="<%= company_text_logo_url %>" alt="<%= company_full_name %>">
			</div>
		</div>
	</div>
</header>